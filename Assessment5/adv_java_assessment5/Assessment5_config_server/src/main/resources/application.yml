server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Manideep12222385/Assessment5_config_server
          default-label: main
          clone-on-start: true